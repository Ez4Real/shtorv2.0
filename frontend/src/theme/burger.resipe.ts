import { defineRecipe } from "@chakra-ui/react"

export const buttonRecipe = defineRecipe({
    base: {

    },
    variants: {
        variant: {
            plain: {
                bg: "transparent",
            },
        },
    },
})
